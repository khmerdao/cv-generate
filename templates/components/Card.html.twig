<section {{ attributes.defaults({class: this.classes()}) }}>
    {% if title %}<h2 class="mb-4 font-display text-base font-semibold">{{ title }}</h2>{% endif %}
    {% block content %}{% endblock %}
</section>
