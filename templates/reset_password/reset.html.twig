{% extends 'layout/marketing.html.twig' %}
{% block content %}
<twig:Card title="{{ 'auth.change_password'|trans }}" class="mx-auto max-w-md">
    {{ form_start(form) }}
    {{ form_errors(form) }}
    <twig:Input :field="form.plainPassword.first" />
    <twig:Input :field="form.plainPassword.second" />
    {{ form_row(form.submit, {label: 'auth.change_password'|trans, attr: {class: 'inline-flex items-center justify-center rounded-full bg-ink px-6 py-3 text-sm font-semibold text-white transition hover:bg-ink/90'}}) }}
    {{ form_end(form) }}
</twig:Card>
{% endblock %}
