{% extends 'layout/marketing.html.twig' %}
{% block content %}
<twig:Card title="{{ 'auth.forgot_password'|trans }}" class="mx-auto max-w-md">
    {{ form_start(form) }}
    {{ form_errors(form) }}
    <twig:Input :field="form.email" />
    {{ form_row(form.submit, {label: 'auth.send'|trans, attr: {class: 'inline-flex items-center justify-center rounded-md bg-accent px-4 py-2 text-sm font-medium text-white transition hover:bg-accent-hover'}}) }}
    {{ form_end(form) }}
</twig:Card>
{% endblock %}
