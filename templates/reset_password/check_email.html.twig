{% extends 'layout/marketing.html.twig' %}
{% block content %}
<twig:Card title="{{ 'auth.check_email_title'|trans }}" class="mx-auto max-w-md">
    <p class="text-sm text-ink-muted">{{ 'auth.check_email_body'|trans({'minutes': resetToken.expirationMessageData.minutes ?? 60}) }}</p>
</twig:Card>
{% endblock %}
